name: gesture_recorder
description: A gesture recorder for generating and using test gestures
version: 0.0.1

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test: any
  e2e: 0.5.0+1

dev_dependencies:
  pedantic: ^1.8.0
